// Deprecated: use @/components/layout/SiteNav directly — it matches the
// real homepage nav exactly. Kept as a re-export only so nothing breaks if
// still imported elsewhere.
export { default } from './SiteNav'
